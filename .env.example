# Local development — use a credentials file (not committed to git)
PORT=8000
GOOGLE_SHEETS_ID=your-spreadsheet-id
GOOGLE_SHEET_NAME=Sheet1
GOOGLE_CREDENTIALS_FILE=./google-credentials.json
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://murban-frontend.onrender.com,https://murban.in,https://www.murban.in

# Render — set GOOGLE_CREDENTIALS_JSON instead (single-line JSON, set in dashboard):
#   jq -c . google-credentials.json | pbcopy
